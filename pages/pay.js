import Layout from '@/components/Layout';
import Loader from '@/components/loader/Loader';

function Pay() {
  return (
    <Layout>
      <div>
        <Loader />
      </div>
    </Layout>
  );
}

export default Pay;
